# 美心 AI 行政助手开发规范

本文件是仓库最高级别的开发约定。任何参与本项目的开发者或 AI Agent 在分析、修改、测试代码前都必须完整阅读本文件和 `PROJECT_CONTEXT.md`。若子目录出现更具体的 `AGENTS.md`，在不冲突的前提下叠加执行。

## 1. 项目目标

为美心教育机构建设一套可长期使用的内部行政系统，逐步替代学生、课表、签到、课消、缴费、教师课时和工资基础核算中的重复 Excel 统计。系统服务真实业务，不以演示效果、技术炫技或论文完整性为目标。

成功标准：行政人员无需技术背景即可完成日常操作；关键数据可追溯、可纠错、可备份；核心流程有自动化测试；系统可在低成本环境稳定部署。

## 2. 用户画像与权限

- 行政人员：高频用户，负责资料维护、排课、签到复核、缴费登记、导入和查询。界面必须使用业务语言，减少必填项和多层操作。
- 教师：查看个人课表、登记或确认签到、查看本人课时统计。不得访问无关学生财务信息或其他教师工资。
- 负责人：查看全部数据、经营报表、异常提醒、工资核算与系统设置，负责最终审核。

所有新增功能必须明确谁使用、在什么场景使用、最少需要几步、误操作如何恢复。

## 3. 开发原则

1. 业务正确性优先于功能数量，数据可追溯优先于界面效果。
2. 保持单体架构，未经真实容量或维护问题证明，不引入微服务、消息队列、Kubernetes 或多套前端工程。
3. 先做行政每天使用的主流程，再做智能辅助；AI 不得成为账务、课消或工资计算的唯一决策者。
4. 自动计算必须可解释、可复核、可人工更正；更正需保留原记录和操作人。
5. 金额使用 `Decimal/NUMERIC`，课时使用定点小数，禁止使用二进制浮点数进行结算。
6. 时间在数据库中统一存带时区时间戳，业务显示默认 `Asia/Shanghai`；自然日按机构时区计算。
7. 删除业务数据默认采用停用、作废或冲正，不物理删除已参与课消、缴费、工资核算的数据。
8. 未确认“校精灵”开放接口前，不假设能够实时同步；首期以 Excel 导入和人工复核为可靠接入方式。

## 4. 技术原则

- 后端：Python 3.12、FastAPI、SQLAlchemy 2、Alembic、Pydantic。
- 前端：Jinja2 服务端模板 + HTMX + 少量原生 JavaScript；统一中文界面与响应式布局。
- 数据库：生产 PostgreSQL；本地开发和单人试用允许 SQLite。SQL 与模型必须兼容 PostgreSQL。
- 测试：pytest；API 使用 FastAPI TestClient；关键业务规则使用纯单元测试，数据库流程使用集成测试。
- 依赖通过 `pyproject.toml` 管理并锁定兼容范围；密钥只从环境变量读取。
- 默认一个可部署应用进程和一个数据库，不把 AI 调用放入核心事务。

分层约定：路由只处理 HTTP；服务层承载业务规则和事务；仓储/模型层负责持久化；模板不执行关键业务计算。

## 5. 文件与命名规范

- `backend/app/api/`：路由；`services/`：业务逻辑；`models/`：ORM；`schemas/`：输入输出模型；`core/`：配置、安全和基础设施。
- `frontend/templates/`：页面模板；`frontend/static/`：样式、图片和少量脚本。
- `database/migrations/`：Alembic 迁移；任何结构变更都必须提交迁移。
- `tests/`：按模块组织测试；测试文件使用 `test_*.py`。
- `docs/`：面向维护者和业务负责人的长期文档。
- Python 文件、变量、数据库列使用英文 `snake_case`；类名使用 `PascalCase`；页面文案和业务说明使用简体中文。
- 单个模块只承担一个清晰职责；禁止将全部模型、路由或服务堆入单一文件。

## 6. 数据规范

- 主键默认 UUID；面向用户的编号（学生编号、收据号等）另设唯一业务字段，不暴露主键。
- 所有业务表包含 `created_at`、`updated_at`；关键流水还包含 `created_by`、状态和备注。
- 学生“剩余课时”是账户流水的汇总结果，不允许作为唯一事实来源直接随意修改。
- 签到、课消、缴费、教师课时、工资均使用状态机；自动动作必须有幂等键或唯一约束，防止重复扣课和重复计薪。
- 重要导入保存批次、原始文件哈希、行号、校验结果和操作者，支持失败行导出与重复导入识别。
- 联系方式、工资、缴费属于敏感信息；日志不得输出完整手机号、密钥、工资明细或上传文件内容。
- 生产环境必须每日自动备份数据库，至少每季度验证一次恢复流程。

## 7. 测试要求

每个模块交付时至少具备：

- 正常流程、边界条件、权限拒绝和重复提交测试。
- 可重复创建的测试数据，测试不得依赖生产数据或执行顺序。
- 涉及课消、缴费、课时和工资时，必须覆盖事务回滚、作废/冲正和并发重复请求。
- Excel 导入必须覆盖缺列、错误类型、重复业务编号、部分失败和中文表头。
- 页面至少完成桌面端主流程检查；关键页面补充端到端测试。

合并前最低门槛：测试通过、迁移可从空库执行、应用可启动、文档同步更新。修复缺陷时先补能复现问题的测试。

## 8. Git 提交规范

- 从短生命周期分支开发，保持提交小而可回滚；不得提交 `.env`、数据库文件、上传原件、密钥或真实个人信息。
- 提交信息使用 Conventional Commits：`feat:`、`fix:`、`docs:`、`test:`、`refactor:`、`chore:`。
- 每个提交只表达一个目的，例如 `docs: 完成数据库领域模型设计`。
- 提交前检查 `git diff`、运行相关测试；禁止通过跳过测试掩盖失败。
- 数据库破坏性迁移必须说明备份、兼容窗口和回滚方案。

## 9. 文档维护规范

- 架构、角色、模块边界变化时更新 `docs/系统设计文档.md`。
- 表、字段、约束或状态变化时同时更新 `docs/数据库设计.md` 和迁移。
- 阶段范围、验收标准或上线方式变化时更新 `docs/开发路线.md`。
- 新环境变量、启动命令或部署步骤变化时更新 `README.md` 和 `.env.example`。
- 决策发生重大变化时，在对应文档记录日期、原因、替代方案和影响，避免只改代码不留依据。

## 10. 完成定义

一项工作只有在代码、测试、迁移、中文界面、异常提示、权限、审计和文档均达到该项需求的合理范围后才算完成。不得把占位按钮、静态假数据或未连接的页面报告为已完成业务功能。
